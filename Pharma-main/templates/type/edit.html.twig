{% extends 'base.html.twig' %}

{% block title %}Edit Type{% endblock %}

{% block body %}
    <h1 class="card-title fw-semibold mb-4" >Modifier un type</h1>
    <a href="{{ path('app_type_index') }}" class="btn btn-outline-primary mb-4">< Retour</a>
    <div class="card">
        <div class="card-body">
            {{ include('type/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>
    </div>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}
