{% extends 'base.html.twig' %}

{% block title %}New Type{% endblock %}

{% block body %}
    <h2 class="card-title fw-semibold mb-4">Créer un nouveau type de médicament</h2>
    <a href="{{ path('app_type_index') }}" class="btn btn-outline-primary mb-4">< Retour</a>
    <div class="card">
        <div class="card-body">
            {{ include('type/_form.html.twig') }}
        </div>
    </div>

{% endblock %}
